machine:
  services:
    - docker
dependencies:
  override: 
    - make vendor
test:
  override:
    - make test
deployment:
  staging:
    branch: master
    commands:
      # build image and push to Dockerhub
      - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS
      - make release
